var nav = document.getElementById("nav");

// Adding event listeners to toggle the nav
var openNav = getElementById("nav-open").addEventListener("click", function () {
  nav.style.right = 0;
});

var closeNav = getElementById("nav-close").addEventListener(
  "click",
  function () {
    nav.style.right = 0;
  }
);
